using UnityEngine;

public enum ItemActionType
{
    Cut,      // NailClipper
    Squeak,   // RubberDuck
    Open,     // CanOpener
    Place,    // Potato
    Disable,  // Shoe
    Special01,  // For the Special item found inside the bomb(for example - ticket). I give it the numer, as we could need more
    Empty,  //NoItem
}
